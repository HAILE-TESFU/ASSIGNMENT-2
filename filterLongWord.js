"use strict";
/**
 * @param  {String} str is
 * @returns {String} to
 */
function filterLongWords(str){
    str=["hail","tekl","zerai","tesfom",5];
    
    // eslint-disable-next-line no-undef
    let filteredWords=str.filter((str)=>(str[i].length>5));

    return filteredWords;
}

